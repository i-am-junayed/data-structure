def fill_array(size, list):
  for i in range(size):
    x = int(input(f'Enter the value in {i} th index : '))
    my_list.append(x)
  return list

my_list =[]
n = int(input("Enter the size of the array :"))
print("Here is the filled array : ", fill_array(n, my_list))
